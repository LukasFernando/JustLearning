
on: issues

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: python -m pip install --upgrade pip
      - name: Runnig python
        uses: LukasFernando/JustLearning@main
#         issue-name: "Teste"
#         run: python add_issue_on_readme.py
        
